# Status snapshots

Real `/hermes-status` outputs from cc-forge projects mid-sprint. One
file per snapshot, named `YYYY-MM-DD-<project>-status.md`.

Goal: calibrate what "in-progress" actually looks like — stage,
backlog %, open flags, next-task pointer.
