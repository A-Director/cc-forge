# Gate review captures

Real `/hermes gate review` outputs from cc-forge projects. One file
per review, named `YYYY-MM-DD-<project>-<persona>.md`.

Goal: show what each persona actually emits — output structure,
finding density, severity distribution. Captures here include both
PASS and BLOCK examples so first-time users see the full range.
